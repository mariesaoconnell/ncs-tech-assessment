import { createChatBotMessage } from "react-chatbot-kit";

const config ={
  initialMessages:[createChatBotMessage(`Hi, I'm Finn, VIN Finder's AI! 👋 For a list of questions you can ask me, say "FAQs". `)]
}

export default config;
